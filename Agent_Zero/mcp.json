{
  "mcpServers": {
    "workiq": {
      "command": "npx",
      "args": ["-y", "@microsoft/workiq", "mcp"],
      "tools": ["*"],
      "description": "Microsoft 365 MCP server — provides access to emails, Teams messages, meetings, documents, and SharePoint via the Microsoft Search API."
    }
  }
}
