# Gremium-STATE: Agent Zero → Agency-Brain

Aktualisiert: 2026-07-08 (FINAL-4 nach 9H) · Branch: `feature/agency-brain`

## Status: BATCH 9 „ENTFESSELUNG" RE-AUDIT FINAL-4 NACH 9H **AUDIT9: PASS-WITH-LIMIT attachment-content-not-indexed** (AUDIT-BATCH-9.md) — Live-Direktvergleich per Brief §5 am Seestrasse-Task, gegen HEAD `3419a34` „Report Batch 9H result" (nach 9H-Commit `a95f24c` „Add node obsolete marker and attachment index retry"). **Kontext:** System **warm**. Keine Agent-Zero-Nutzer-Instanz aktiv (`:3000` refused, Lock-PID stale/unangetastet); der einzige `node server.js` (PID **10704**) ist eine **Task-Zero**-Instanz (`E:\Task_Zero 03`, mail-worker-Child) — nicht angetastet. Isolierte Kind-Instanz (Port **3151**, Wert-Lauf-PID **36276**, `_audit9-server.mjs`, agency, v5.0.0, Build `3419a34`; fünf Env-Patches `AZ_AUDIT_TASKS_FILE`/`AZ_AUDIT_JOBS_FILE`/`AZ_AUDIT_LOCK_FILE`/`AZ_AUDIT_NO_DETECT=1`/`AZ_AUDIT_NO_REAPER=1` → eigener audit9-Lock, isolierte Kopien → echte `tasks.json` **nie geöffnet**, SHA-256 vor/nach identisch `BB9CE3D4…C2CF6A4`). Exakter Chat-Auftrag „Suche in der Inbox nach Updates und aktualisiere die PM-View-Felder." per `POST /api/tasks/proj-seestrasse-356/log` (POST 13:40:02.339Z) → Fast-Job `9577bae0` → deep_verify `8ad20556`. **9H behebt den 9G-Kerndefekt (d) VOLLSTÄNDIG und verbessert (a) messbar. (d) PASS:** der veraltete **AV-Go-Live 1. Jul 2026** ist jetzt **`state="obsolete"`** mit **`obsoleteReason="target date passed without completion evidence — needs re-plan"`**, gebucht durch den **eigenständigen `NODE_OBSOLETE`-Marker des Primärlaufs** (`history[type=node-obsolete]` ts=13:40:26). Der Reality-Gateway hat den Marker **approved (held 0)** — die 9G-„Sippenhaft" (Obsolet-Disposition ging mit einer gehaltenen groben `PROJECT_UPDATE` verloren) ist **beseitigt**, weil `NODE_OBSOLETE` nicht mehr an Completion-/`userActions`-Checks gekoppelt ist; das Temporal-Gate erkennt ihn via `nodeObsoleteHandles` als adressierend (`addressed 2/6`); der Applier bucht **nur** `state="obsolete"` + `obsoleteReason` (keine `pmStatus`-Ersetzung, kein Completion-Assert). **(a) PASS-WITH-LIMIT (content-not-indexed-Fall):** der Agent enumerierte + probte den Anhang „Seestrasse August Works Comms.pdf" und **wiederholte einmal** mit alternativer Formulierung (`attachmentIndexAttempts:2`); der M365-Index gab **auch nach dem Retry keinen** Deck-Inhalt her → **ehrlich** `attachmentsHandled:"failed(content-not-indexed)"` **persistiert** (Ledger 19 → 21, `reprobeNextScan:true`) + reviewQueue-Re-Probe-Eintrag („attachment not indexed yet — re-probe next scan"). **Keine unbelegte Slide-Behauptung** (PIS 31. Aug / Port-Counts / AV-Steps NICHT asseriert; `workiq-index` 0×; PIS/„31 Aug"-Vorkommen im State **6 = 6** identisch zur Baseline). Anders als 9G (Ledger blieb 19, nicht persistiert) wird die failed-Disposition jetzt persistiert. **(b) PASS:** `markersApplied 4` (> 0), Gateway approved 4/held 0, granulare Quality-Gate held 0/review 1, granulares Temporal-Gate `ok:false, staleNodes 6, addressed 2, held 0, review 4`; kein Batch-Hold; reviewQueue +6 (78→84); pmStatus „August cabling 17-28 Aug" → `confirmed`, mehrere waitingOn → `confirmed` (evidenzbelegt). **(c) PASS:** Stufe-1 = **23,9 s** (warm, `mcpMode:'none'`); Warm-Nachmessung 22,0 s; konsistent mit 9F 22,4 s / 9G 23,1 s. **Kern-Befund:** 9H schließt beide 9G-Rest-Defekte — (d) via `NODE_OBSOLETE`-Minimal-Marker mit schmaler Gateway-Prüfung (Primärlauf bucht die Go-Live obsolet, sauber attribuiert), (a) via Anhang-Index-Retry + Re-Probe-Persistenz (failed(content-not-indexed) jetzt im Ledger + reviewQueue). Einziges Rest-Limit: Umgebungsvarianz (M365-Index exponierte diesen Lauf keinen Anhang-Inhalt) — ehrlich behandelt. **Timing-Baseline:** POST→Stufe-1 23,9 s · POST→Stufe-2-Antwort 465,3 s (≈7,75 min) · POST→Marker-Verarbeitung 565,7 s (≈9,43 min, applied 4/held 0); Phasen: Queue ~0,07 s / Stufe-1-Brain 23,9 s / Deep-Enqueue→brain_run ~0 s (kein Block) / Discovery+Anhang+Retry+Reasoning ~441,3 s / Gateway+granulare Gates+Apply ~100,4 s; **6 WorkIQ-Calls** (Abschlüsse bei brain-elapsed 78/123/123/170/170/220 s), kein 40/150-Tool-Budget-Ereignis. **Transparenz (Erst-Lauf verworfen):** ein erster Primärlauf wurde nicht gewertet, weil eine dazwischen abgesetzte Warm-Nachmessung während des Primär-Deep-Fensters die 1-fach-agency-Semaphore staute → Primär-Apply blieb `scheduled`, während die Nachmessung selbst die Obsolet-Buchung auslöste (keine saubere Attribution; dabei ein API-Latenz-Spike der Stufe-1 von 109,8 s unter zwei fremden opus-agency-Prozessen, ohne 9H-Bezug). Deshalb: State auf Baseline zurückgesetzt (Re-Copy echte `tasks.json`, SHA `BB9CE3D4…`), Kind neu gestartet, Primärlauf ohne konkurrierende Query bis zum Ende gefahren — alle berichteten Zahlen aus diesem sauberen Lauf (deep `8ad20556`). **Sicherheit:** kein STOP/START, keine breiten/namensbasierten Kills (`taskkill` nicht verwendet, nur `Stop-Process -Id`), nur eigene Kind-PIDs (34980/36276 + eigene Warm-up-/verworfene agency-Streu-Kinder 11704/34184) beendet (legitime Primär-Deep-Kinder self-exited); Task-Zero PID 10704 + `:3000`-Lock + echte tasks.json/jobs.json + fremde agency.exe (24424/36840/36748) unangetastet; Temp-Harness (`_audit9-server.mjs`, alle 10 brain-work-Run-Dirs beider Läufe, audit9-Session-Kopien, audit9-Lock, Logs) rückstandsfrei entfernt (fremde 07.07-Run-Dirs unangetastet); `git status` sauber, `npm test` grün (**153/153**); nur zwei Docs (AUDIT-BATCH-9.md, STATE.md) geändert.

## Vorheriger Stand: BATCH 9 „ENTFESSELUNG" RE-AUDIT FINAL-3 NACH 9G **AUDIT9: FAIL a,d (historisch — durch 9H-FINAL-4 überschrieben)** (AUDIT-BATCH-9.md) — Live-Direktvergleich per Brief §5 am Seestrasse-Task, gegen HEAD `11cd092` „Document Batch 9G prompt hardening" (nach 9G-Commit `a1accfe` „Granularize temporal gate handling"). **Kontext:** System **warm** (kein Post-Reboot). Keine Agent-Zero-Nutzer-Instanz aktiv (`:3000` refused; Lockfile-PID 21644 stale/tot vom 2026-07-07, unangetastet); der einzige `node server.js` (PID **10704**, gestartet 13:14) ist eine **Task-Zero**-Instanz (`E:\Task_Zero 03`, Child `mail-worker.ps1`) — anderes Projekt, nicht angetastet. Isolierte Kind-Instanz (Port **3151**, PID **30228**, `_audit9-server.mjs`, agency, v5.0.0, Build `11cd092`; fünf Env-Patches `AZ_AUDIT_TASKS_FILE`/`AZ_AUDIT_JOBS_FILE`/`AZ_AUDIT_LOCK_FILE`/`AZ_AUDIT_NO_DETECT=1`/`AZ_AUDIT_NO_REAPER=1` → eigener audit9-Lock, isolierte Kopien im Session-Ordner → echte `tasks.json` **nie geöffnet**, SHA-256 vor/nach identisch `BB9CE3D4…C2CF6A4`, Kopie änderte sich `BB9CE3D4…`→`E594DF38…`). Exakter Chat-Auftrag „Suche in der Inbox nach Updates und aktualisiere die PM-View-Felder." per `POST /api/tasks/proj-seestrasse-356/log` (POST 12:16:34.833Z) → Fast-Job `b5702b6e` → deep_verify `40647730`. **9G korrekt verdrahtet und wirksam:** das Temporal-Gate ist jetzt **granular** (`filterMarkersByTemporalPassGate`) — hielt **0 Marker**, wandelte die 5 unadressierten Stale-Nodes in **5 reviewQueue-Hinweise** (`stale date unreconciled: …`) statt All-or-Nothing-Batch-Hold → **erstmals seit 9E wieder Marker persistiert: `markersApplied 3 / held 1`** (die 9F-Temporal-Hold-Blockade ist beseitigt). **Echter Anhang-Fortschritt:** der Agent **enumerierte pro Thread die Anhänge und probte beide** (Laith-Deck + Netzwartungs-Anhang). **ABER (a) FAIL:** beide Anhang-Inhalte kamen diesen Lauf als `failed(content-not-indexed)` zurück (M365-Index gab **keinen** Deck-Inhalt her — Daten-Varianz vs. 9F) → **PIS 31. Aug / Scope-Liste / 24-Port 0×** im State, `workiq-index` 0×, Ledger **19** unverändert (nicht persistiert). Der Agent verhielt sich **ehrlich** („no slide-only timeline items are asserted"). **(d) FAIL:** der Agent **emittierte diesen Lauf erstmals** die Obsolet-Entscheidung — Prosa („AV Go-Live 1 Jul 2026 … **marked obsolete (needs re-plan)**") **und** als `PROJECT_UPDATE`-Marker — **aber der Reality-Gateway HIELT genau diese eine pmStatus-tragende PROJECT_UPDATE** (`heldMarkers 1`: „**PM status claims completion while waitingOn is still populated**" + „**Batch 7 action gate: pmStatus.userActions[0].askQuote requires verbatim text, from, and date**"). Da die Obsolet-Disposition den approved-Marker-Satz nie erreichte → Temporal-Gate `staleNodes 6 / addressed 1` → State unverändert `pmStatus.planned` „AV Go-Live target 1 Jul 2026" `unconfirmed`, **kein** `obsoleteReason`; `li-see-av-commissioning` unverändert; `1 Jul 2026` 23× im State. Der Stale-Node ist nun immerhin reviewQueue-Eintrag „stale date unreconciled", erfüllt (d) aber nicht. **(c) PASS:** Stufe-1 = **23,1 s** (warm, `mcpMode:'none'`); Warm-up-Trivial ≈**109 s** (kalter Erst-Spawn, ohne (c)-Einfluss). **(b) PASS:** Update-Disziplin granular wirksam — Gateway approved 4/held 1, granulare Quality-Gate held 0/review 4, granulares Temporal-Gate `ok:false, staleNodes 6, addressed 1, held 0/review 5`, `applyMarkerBatch` → **`markersApplied 3`** (2 dropped) — **markersApplied > 0 erfüllt**; reviewQueue +10 (78→88); kein falscher State. **Kern-Befund:** Root-Cause kippt erneut — von „Temporal-Gate hält den GESAMTEN Batch" (9F) zu **zwei entkoppelten Rest-Defekten**: (d) der Agent tut jetzt das Richtige (Obsolet-`PROJECT_UPDATE`), aber **alle pmStatus-Mutationen leben in EINER groben PROJECT_UPDATE**, die der Reality-Gateway aus einem **unabhängigen** Grund (completion-while-waitingOn + fehlende verbatim-Evidenz auf userActions) hält → Obsolet-Disposition geht mit der ganzen Marker verloren; (a) reine **Daten-Varianz** (Index exponierte keinen Anhang-Inhalt). Non-blocking: Obsolet-/Supersede-Dispositionen in **dedizierten Marker** trennen (nicht mit Completion-/userActions-Checks koppeln) ODER Gateway die nicht-completion-Teile einer teils-gehaltenen PROJECT_UPDATE **partiell anwenden** lassen ODER Completion-Check lockern, wenn die Marker einen Node **obsolet** markiert. **Warm-up-Anmerkung (transparent):** der Trivial-Warm-up „Summarize this task in two sentences." löste `deepVerification.required=true` aus (Streu-Deep-Job `9a7bc6d3`, storniert, agency-Child PID 31108→**29504** vor Marker-Apply per PID beendet, keine Kontamination — isolierter State nach Warm-up = Baseline). **Diesen Lauf KEIN Semaphore-Block** (frühe Kill → Primär-Deep lief sofort bei 23,1 s an). **Timing-Baseline:** POST→Stufe-1 23,1 s · POST→Stufe-2-Antwort 579,5 s (≈9,66 min) · POST→Marker-Verarbeitung 809,5 s (≈13,49 min, marker_apply_done applied 3/held 1); Phasen: Queue ~0,1 s / Stufe-1-Brain 23,0 s / Deep-Enqueue→brain_run ~0 s (kein Block) / Discovery+Anhang+Reasoning ~556,4 s / Gateway+granulare Gates+Apply ~230,0 s; **10 WorkIQ-Calls** (Abschlüsse bei brain-elapsed 70,0/139,1/150,5/202,6/274,9 s), kein 40/150-Tool-Budget-Ereignis. **Sicherheit:** kein STOP/START, keine breiten/namensbasierten Kills (`taskkill` nicht verwendet, nur `Stop-Process -Id`), nur eigene PIDs 30228 (Child) + 35812 (conhost) + 35788 (SSE-Recorder) + 29504 (Warm-up-agency-Streu-Kind) per PID beendet (legitime Primär-Deep-Kinder 29164/33308 self-exited); Task-Zero PID 10704 + `:3000`-Lock (PID 21644) + echte tasks.json/jobs.json + zwei **fremde** agency.exe (24424 Parent 11216, 2968 Parent 27464) unangetastet; Temp-Harness (`_audit9-server.mjs`, 4 brain-work-Run-Dirs, audit9-Session-Kopien, audit9-Lock) rückstandsfrei entfernt; `git status` sauber, `npm test` grün (150/150); nur zwei Docs (AUDIT-BATCH-9.md, STATE.md) geändert.

## Vorheriger Stand: BATCH 9 „ENTFESSELUNG" RE-AUDIT FINAL-2 NACH 9F **AUDIT9: FAIL a,d (historisch — durch 9G-FINAL-3 überschrieben)** (AUDIT-BATCH-9.md) — Live-Direktvergleich per Brief §5 am Seestrasse-Task, gegen HEAD `1db83fb` „Report Batch 9F result" (nach 9F-Commits `fb78f0c` „Granularize processing ledger quality gate" + `5028fd2` „Cover granular quality and temporal gates"). **Kontext:** System **warm** (kein Post-Reboot). Keine Agent-Zero-Nutzer-Instanz aktiv (`:3000` refused; Lockfile-PID 21644 stale/tot, unangetastet); der einzige `node server.js` (PID **10704**) ist eine **Task-Zero**-Instanz (`E:\Task_Zero 03`, mail-worker-Child) — anderes Projekt, nicht angetastet. Isolierte Kind-Instanz (Port **3151**, PID **35640**, `_audit9-server.mjs`, agency, v5.0.0; fünf Env-Patches `AZ_AUDIT_TASKS_FILE`/`AZ_AUDIT_JOBS_FILE`/`AZ_AUDIT_LOCK_FILE`/`AZ_AUDIT_NO_DETECT=1`/`AZ_AUDIT_NO_REAPER=1` → eigener audit9-Lock, isolierte Kopien im Session-Ordner → echte `tasks.json` **nie geöffnet**, SHA-256 vor/nach identisch `BB9CE3D4…C2CF6A4`, Kopie änderte sich `BB9CE3D4…`→`821D0C33…`). Exakter Chat-Auftrag „Suche in der Inbox nach Updates und aktualisiere die PM-View-Felder." per `POST /api/tasks/proj-seestrasse-356/log` (POST 11:25:58.788Z) → Fast-Job `6acbcd60` → deep_verify `8dbef21b`. **9F korrekt verdrahtet und wirksam:** (1) die Batch-7-Quality-Gate ist **granular** — hielt **0 Marker**, wandelte den Ledger-Count-Mismatch (`expected 1, got 3` für die Temp-Workspace-Konversation) in **einen reviewQueue-Hinweis** (`heldMarkers 0, reviewItems 1`), **kein All-or-Nothing-Batch-Hold mehr**; (2) das Temporal-Gate läuft **entkoppelt nach** dem Quality-Filter. **Echter Anhang-Fortschritt diesen Lauf (erstmals):** der Agent **enumerierte den Anhang per Dateiname** („One attachment enumerated: **Seestrasse August Works Comms.pdf**") und **erntete PIS 31. August 2026** (Yardstick-Meilenstein) + „AV rooms unavailable ab Fr 14 Aug" + Closure 17–28 Aug per gezielter WorkIQ-Anhang-Abfrage. **ABER (a) FAIL:** (i) Scope-Liste (Cable room optim / Security System renewal / **AV life cycle renewal**) + 24-Port **nicht** geerntet; (ii) **nichts persistiert** — `markersApplied 0`, `workiq-index` 0× im State, `PIS`/`31 Aug` nur im angehängten Antworttext (2×/2×), `pmStatus`/`lineItems` unverändert, Ledger bleibt 19. **(d) FAIL:** der Agent **erkannte** „AV go-live 1 Jul 2026 has passed", entschied sich aber bewusst gegen Supersession („**I am retaining that node and flagging it for review rather than asserting a change**"; Begründung: keine frische Post-Cursor-Bestätigung, WorkIQ-Bodies expired) → **kein** Supersede-Marker → Temporal-Gate `staleNodes 6 / addressed 1` **hielt ALLE 5** Marker (All-or-Nothing auf Temporal-Ebene) → State unverändert `pmStatus.planned` „AV Go-Live target 1 Jul 2026" `unconfirmed`, kein `obsoleteReason`; `li-see-av-commissioning` unverändert; `1 Jul 2026` 9× im State. **(c) PASS:** Stufe-1 = **22,4 s** (warm, `mcpMode:'none'`), Warm-up-Trivial = **22,0 s** — beide < 90 s (Maschine warm; die 9E-115,5-s-Verfehlung war post-reboot-bedingt). **(b) PASS:** Update-Disziplin intakt/granularer — Gateway approved 5/held 0, granulare Quality-Gate held 0/review 1, Temporal-Gate-Hold (korrekt), reviewQueue +2 (Quality- + Temporal-Hinweis, `ref=proj-seestrasse-356`), kein falscher State. **Kern-Befund:** Root-Cause kippt erneut — von „All-or-Nothing-**Quality**-Gate verwirft gute Arbeit" (9E) zu „Agent enumeriert Anhang + erntet PIS 31 Aug, **behält** aber die stale Daten bewusst (1/6 adressiert), und das **Temporal-Gate hält bei Fehlschlag weiterhin den GESAMTEN Batch**" → auch der gut-geformte Anhang-Marker gehalten. Non-blocking: Temporal-Gate ebenfalls granular machen (nur stale-node-berührende Marker halten) ODER Deep-Prompt zwingt für jeden nicht-bestätigbaren stale Date einen expliziten Supersede-Marker mit `obsoleteReason`. **Warm-up-Anmerkung (transparent):** der Trivial-Warm-up „Summarize this task in two sentences." löste diesen Lauf `deepVerification.required=true` aus (Streu-Deep-Job `860949b6`, `markerProcessingStatus:"skipped"`, 0 Marker, keine Kontamination — verifiziert); sein agency-Child (PID 31108) wurde vor Marker-Apply per PID beendet; ein 1-fach-agency-Semaphore blockierte den Primär-Deep ~136 s (Harness-Artefakt, kein Produkt-Verhalten, ohne (c)-Einfluss). **Timing-Baseline:** POST→Stufe-1 22,4 s · POST→Stufe-2-Antwort 540,1 s (≈9,00 min) · POST→Marker-Verarbeitung 649,7 s (≈10,83 min, marker_apply_held); Phasen: Queue ~0,04 s / Stufe-1-Brain 22,4 s / Deep-Enqueue→brain_run 136,5 s (Semaphore-Block, Harness) / Discovery+Anhang+Reasoning ~381,2 s / Gateway+Gate-Hold ~109,5 s; effektiv ohne Block ≈513 s (8,55 min); 4 WorkIQ-Calls (Abschlüsse bei brain-elapsed 208,8/241,2/262,1/323,3 s), kein 40/150-Tool-Budget-Ereignis. **Sicherheit:** kein STOP/START, keine breiten/namensbasierten Kills (`taskkill` nicht verwendet, nur `Stop-Process -Id`), nur eigene PIDs 35640 (Child) + 31720 (conhost) + 27708 (SSE-Recorder) + 30332 (Driver, self-exited) + 31108 (Warm-up-agency-Streu-Kind) per PID beendet; Task-Zero `:? ` PID 10704 + `:3000`-Lock (PID 21644) + echte tasks.json/jobs.json + Copilot-CLI-WorkIQ- (16760/3120/31716/35960) und Playwright-MCP-Kinder (9096/20528) unangetastet; Temp-Harness (`_audit9-server.mjs`, brain-work-Run-Dirs, audit9-Session-Kopien, audit9-Lock) rückstandsfrei entfernt; `git status` sauber, `npm test` grün; nur zwei Docs (AUDIT-BATCH-9.md, STATE.md) geändert.

## Vorheriger Stand: BATCH 9 „ENTFESSELUNG" RE-AUDIT FINAL NACH 9E **AUDIT9: FAIL a,c,d (historisch — durch 9F-FINAL-2 überschrieben)** (AUDIT-BATCH-9.md) — Live-Direktvergleich per Brief §5 am Seestrasse-Task, gegen HEAD `723eba6` (nach 9E-Commit `06c35bb` „Unify chat deep verification gates"). **Kontext:** Neustart nach Computerausfall (Vorläufer-Lauf starb ~09:15 vor Verdikt, Harness-Reste entfernt); Nutzer-Instanz `:3000` war **down** (Lockfile-PID 21644 stale, unangetastet), keine zu schützende User-Instanz. Isolierte Kind-Instanz (Port **3131**, PID **25204**, `_audit9-server.mjs`, agency, build 723eba6; fünf Env-Patches `AZ_AUDIT_TASKS_FILE`/`AZ_AUDIT_JOBS_FILE`/`AZ_AUDIT_LOCK_FILE`/`AZ_AUDIT_NO_DETECT=1`/`AZ_AUDIT_NO_REAPER=1` → eigener audit9-Lock, isolierte Kopien → echte `tasks.json` nie geöffnet, SHA-256 vor/nach identisch `BB9CE3D4…C2CF6A4`, Kopie änderte sich `BB9CE3D4…`→`D552F881…`). Exakter Chat-Auftrag „Suche in der Inbox nach Updates und aktualisiere die PM-View-Felder." per `POST /api/tasks/proj-seestrasse-356/log` (POST 10:18:35.392Z) → Fast-Job `da9898ed` → deep_verify `61e74fc3`. **9E korrekt verdrahtet** (live bestätigt: `marker_apply_held`-Event + reviewQueue-Gate-Hinweis `ref=proj-seestrasse-356`). **Der Agent behob diesen Lauf beide Vor-9E-Verhaltensdefekte:** er **öffnete den PDF-Anhang** per gezielter WorkIQ-Abfrage (Antwort verbatim „The attachment probe returned concrete, decision-relevant facts" / „attachment read via the M365 index", Ledger `ledgerItems:1`) **und** erkannte + superseedete den veralteten **AV-Go-Live 1. Jul** in der Antwort („Stale planned date … must confirm or supersede" → „superseded the stale 'AV Go-Live 1 Jul 2026' … folded into August scope"). **ABER das Batch-7-Ledger-Quality-Gate hielt den GESAMTEN 4-Marker-Batch** (`missing ledger disposition for enumerated item teams:teams-martin-sharepointdocs-20260706-1632`, `ledgerItems:1`): Reality-Gateway approved 3 / held 1, dann Quality-Gate-Hold → `markersApplied 0 / markersHeld 4` → **nichts persistiert**. **(a) FAIL:** Anhang geprobt (Fortschritt), aber (i) exakte Yardstick-Fakten **PIS 31. Aug + Scope-Liste (Cable room optim / AV life cycle)** + 24-Port **nicht** geerntet (anderer/summierter Deck-View „Seestrasse August Works Comms.pdf" → surfte 17–28-Aug-Closure / July-patch-panel / CorpNet-in-progress / PC-move-17-Aug), (ii) Ledger `attachmentsHandled='yes(workiq-index)'` emittiert aber **gehalten** → nicht persistiert (`workiq-index` 0× im State, `PIS/31 Aug` 0×). **(d) FAIL:** Go-Live in Antwort superseedet (Fortschritt), aber Marker **gehalten** (Quality-Gate läuft **vor** Temporal-Gate, short-circuitet es) → State unverändert `pmStatus.planned` „AV Go-Live target 1 Jul 2026" `unconfirmed`, kein `obsoleteReason`; `li-see-av-commissioning` unverändert; `1 Jul 2026` 7× im State. **(c) FAIL:** Stufe-1 = **115,5 s** (>90 s); Warm-Nachmessung (Trivial-Frage) = **120,8 s** — beide >90 s, **umgebungsbedingt** (Post-Reboot-Last, Stufe-1 spawnt trotz `mcpMode:'none'` agency copilot), **9E-unabhängig** (Vor-Audit warm 28,1 s). **(b) PASS:** Update-Disziplin intakt/strenger — Gateway approved 3/held 1, Quality-Gate-Hold, reviewQueue +1, CorpNet-Signal + ambige Ownership → Review statt State. **Kern-Befund:** Root-Cause kippt von „Agent ignoriert Anhang/bereinigt Stale nicht" (Vor-9E) zu „Agent tut beides, aber All-or-Nothing-Batch-Hold der Ledger-Gate verwirft die gute Arbeit über ein einzelnes ledger-loses Teams-Item". Non-blocking-Empfehlung: Batch-Hold granular (nur ledger-losen Marker halten) ODER Enumerations-/Ledger-Kongruenz im Deep-Prompt härter erzwingen. **Timing-Baseline:** POST→Stufe-1 115,5 s · POST→Stufe-2-Antwort 718,6 s (≈11,98 min, `durationMs=603071`) · POST→Marker-Verarbeitung 1035,9 s (≈17,27 min, marker_apply_held); Phasen: Queue 0,2 s / Stufe-1-Brain 115,3 s / Stufe-2-Spawn+MCP ~24,6 s / Stufe-2-Discovery+Anhang+Reasoning ~578,5 s / Gateway+Hold 317,3 s; 5 WorkIQ-Calls (Abschlüsse bei brain-elapsed 124,7/161,3/191,3/314,0/314,0 s), kein 40/150-Tool-Budget-Ereignis. **Sicherheit:** kein STOP/START, keine breiten/namensbasierten Kills, nur eigene PIDs 25204 (Child) + 17364 (SSE-Recorder) + eigene agency-Deep-Kinder per PID beendet; `:3000`-Lock (PID 21644) + echte tasks.json/jobs.json + Copilot-CLI-node/Playwright-Kinder unangetastet; Temp-Harness (`_audit9-server.mjs`, brain-work-Run-Dirs, audit9-Kopien, audit9-Lock) rückstandsfrei entfernt; `git status` sauber, `npm test` grün; nur zwei Docs (AUDIT-BATCH-9.md, STATE.md) geändert.

## Vorheriger Stand: BATCH 9 „ENTFESSELUNG" RE-AUDIT NACH 9B/9C/9D **AUDIT9: FAIL a,d** (AUDIT-BATCH-9.md, historisch — durch 9E-FINAL überschrieben) — Direktvergleich per Brief §5 am Seestrasse-Task, gegen HEAD `bb1f535` (nach 9B/9C/9D). Isolierte Kind-Instanz (Port **3131**, PID **56932**, `_audit9-server.mjs`, agency, build bb1f535; fünf Env-Patches `AZ_AUDIT_TASKS_FILE`/`AZ_AUDIT_JOBS_FILE`/`AZ_AUDIT_LOCK_FILE`/`AZ_AUDIT_NO_DETECT=1`/`AZ_AUDIT_NO_REAPER=1` → keine :3000-Subprozess-Kills, eigener audit9-Lock, isolierte Kopien → echte `tasks.json` nie geöffnet, SHA-256 vor/nach identisch `BB9CE3D4…C2CF6A4`, Kopie änderte sich `BB9CE3D4…`→`E067A6E2…`). Exakter Chat-Auftrag „Suche in der Inbox nach Updates und aktualisiere die PM-View-Felder." per `POST /api/tasks/proj-seestrasse-356/log` (POST 06:28:11.676Z) → Fast-Job `f2ead96c` → deep_verify `7016c8b7`. **(c) PASS: Stufe-1 = 28,1 s < 90 s** (brain 27,8 s, state-only `mcpMode:'none'`, `…fast-fallback-v1`, non-blocking). **(b) PASS: Update-Disziplin** — Reality-Gateway (xhigh) **approved 6 / held 1**, 5 applied / 2 no-op-dropped; der 1 gehaltene Marker war ein `PROJECT_UPDATE` „completion claims while waitingOn still populated" (korrekt → reviewQueue +1); Netzwartungs-Signal (ZURICH-SEE Aruba POE-AGG, 22. Jul) → Review statt State; `li-see-corpnet` unverändert (NEEDS_REVIEW); die 6 offenen Punkte unberührt. **Stufe-2 = entfesselt** (`mcpMode:'default'`, alle Default-MCPs, 25-min-Fenster): **5 echte WorkIQ-Inbox-Discovery-Calls**, fand den Laith/Belinda-Thread („Confirmation: Temporary Workspace Setup", 07.07. 14:48) und erntete **frisch** den **½-Tag-Vorbehalt Nicolas** (waitingOn `li-see-pc-relocation`). **(a) FAIL: PDF-Anhang „Microsoft Seestrasse - August Works.pdf" NICHT geöffnet** — keine gezielte WorkIQ-Anhang-Abfrage; **PIS 31. Aug / Scope-Liste (Cable room optim / AV life cycle) / 24-Port-Panel = 0 Treffer** im gesamten Task-State; **kein** Ledger-Eintrag dieses Laufs (19/19 vom 06.07., `workiq-index` 0× im File). Der Agent benannte „the deck", las ihn aber nicht — gleiche Fehlerlage wie Erst-Audit. **(d) FAIL: veralteter AV-Go-Live 1. Jul (7 Tage verstrichen) nicht bereinigt** — unverändert in `pmStatus.planned` (`unconfirmed`, kein `obsoleteReason`) + `li-see-av-commissioning`, in Stufe 2 nicht erwähnt. **Root-Cause (Befund, kein Produktcode geändert):** (a) der Deep-Prompt instruiert die Anhang-Abfrage (task-chat.js:594), aber der Agent wählte sie nicht, und der Chat-Deep-Verify-Pfad erzeugt ohnehin keine `processing.ledger`-Dispositionen (dort lebt `attachmentsHandled='yes(workiq-index)'`; 9C/9D wurde im Scan-runBrain-Harness belegt, nicht im Chat-Pfad); (d) `evaluateTemporalPassGate` (`brain/temporal-pass.js`) ist NUR in `brain/scan-brain.js:359` verdrahtet, NICHT im Chat-Deep-Verify → keine erzwungene Stale-Reconciliation im §5-Pfad. **Timing-Baseline:** POST→Stufe-1 28,1 s · POST→Stufe-2-Nachtrag 518,3 s (≈8,64 min, deep answer_posted) · POST→Marker-Apply-Done 776,7 s (≈12,94 min); Phasen: Queue 0,3 s / Stufe-1-Brain 27,8 s / Stufe-2-Spawn+MCP-Startup ~27,8 s / Stufe-2-Brain-Discovery ~462 s / Gateway+Apply 258,4 s (4,3 min); 5 WorkIQ-Calls (Starts bei brain-elapsed 133,9/174,1/174,1/251,0/251,0 s), kein 40/150-Tool-Budget-Ereignis. **Sicherheit:** kein STOP/START, keine breiten/namensbasierten Kills, nur eigene PIDs 56932 (Child) + 57912 (SSE-Recorder) per PID beendet; :3000 (PID 21644, agency, uptime 71 172→72 515 s) + echte tasks.json/Lock/jobs.json unangetastet; Temp-Harness (`_audit9-server.mjs`, brain-work-Run-Dirs, audit9-Lock) rückstandsfrei entfernt; `git status` sauber, `npm test` 146/146; nur zwei Docs (AUDIT-BATCH-9.md, STATE.md) geändert.

## Vorheriger Stand: BATCH 9 „ENTFESSELUNG" ERST-AUDIT **AUDIT9: FAIL a,d** (vor 9B/9C/9D) — Direktvergleich per Brief §5 am Seestrasse-Task. Isolierte Kind-Instanz (Port 3119, PID 44400, `_audit9-server.mjs`, agency, Session-Vars gestrippt, `detectExistingInstance`→null via `AZ_AUDIT_NO_DETECT`, Startup-/Shutdown-Reaper neutralisiert via `AZ_AUDIT_NO_REAPER` → keine :3000-Subprozess-Kills, eigener audit9-Lock, TASKS_FILE/JOBS_FILE isolierte Kopien → echte `tasks.json` nie geöffnet, SHA-256 vor/nach identisch `4F99EB3E…34CB9F9`). Exakter Chat-Auftrag „Suche in der Inbox nach Updates und aktualisiere die PM-View-Felder." per `POST /api/tasks/proj-seestrasse-356/log` → Log-Job `7ef6f365` → deep_verify `3552d835`. **(c) PASS: Stufe-1 = 31,8 s < 90 s** (state-only, `mcpMode:'none'`, Methode `…fast-fallback-v1`, non-blocking). **(b) PASS: Update-Disziplin** — Reality-Gateway (xhigh) approved **7/7** Marker (held 0, dropped 0), nur die wirklich geänderten Knoten angefasst (`li-see-pc-relocation` + `li-see-aug-works` currentState mit 7-Jul-Fakten, `state unconfirmed→confirmed`; pmStatus.current/planned/waitingOn augmentiert; sourceRefs 34→35; reviewQueue 77→78 für das DLP-Netzwartungs-Signal — **nicht** als State behauptet), die 6 unveränderten offenen Punkte unberührt. **Stufe-2 = 636 s ≈ 10,6 min < 25 min**, entfesselt (`mcpMode:'default'`, alle Default-MCPs), fand den Jul-6-Laith-Skeik-Thread („Confirmation: Temporary Workspace Setup", 06.07. 18:45) und erntete **frisch** den **½-Tag-Vorbehalt Nicolas** (Nicolas Ottomüller ~½ Tag Prep 17. Aug, Laith wartet auf Bestätigung) + Belinda hält Mitarbeiter-Kommunikation zurück; korrekt „addressed to Laith, no direct action for Martin". **(a) FAIL: PDF-Anhang nicht geöffnet** — der Agent benannte „an August office-closure communication **deck**" (= den Anhang), **las ihn aber nicht**; die anhang-exklusiven Fakten **PIS 31. Aug** und **24-Port-Panel-Mitigation** fehlen im gesamten Task-State (0 Treffer). (Fakten „17–28 Aug Schließung", „AV-Räume ab 14. Aug", „Temp-Workspace bestätigt" waren bereits vorbestehend im Wahrheitsbaum.) Das ist genau das „Nicht-Nutzen verfügbarer Quellen inkl. Anhängen = Mangel" aus Batch 9 §1/§3. **(d) FAIL: veralteter AV-Go-Live 1. Jul nicht bereinigt** — steht unverändert in `pmStatus.planned` (`state:"unconfirmed"`, „AV Go-Live target 1 Jul 2026 for commissioned rooms") + `li-see-av-commissioning.currentState` („…1 Jul 2026 go-live"), in der Stufe-2-Antwort nicht erwähnt. Kein Tool-Budget-Ereignis (40-Warn/150-Stopp beide nicht gefeuert). **Parallele Session (nicht-blockierend, KEIN Audit-Artefakt):** zeitgleich lief eine separate user-gestartete agency-Executor-Session (PID 29188, `claude-opus-4.8`/xhigh, via bash `git add -A && git commit … && agency copilot …`) mit exakt diesem Batch-9-Auftrag gegen :3000/echte tasks.json — von mir **unangetastet**, ohne Kreuz-Kontamination (Kopie-Isolation :3119). **Sicherheit:** kein STOP/START, keine breiten/namensbasierten Kills, nur eigene Kind-PID 44400 per PID beendet; :3000 (PID 21644, agency, uptime durchgehend) + echte tasks.json/Lock/jobs.json + die parallele Session unangetastet; Temp-Harness (`_audit9-server.mjs`, `…\files\audit9\`, audit9-Lock, brain-work-Run-Dirs) rückstandsfrei entfernt; npm test Baseline 142/142; nur zwei Docs (AUDIT-BATCH-9.md, STATE.md) geändert.

## Vorheriger Stand: S2-SPEED-KURZMESSUNG NACH FIX **S2: FAIL 10.0 min** (AUDIT-TWOTIER.md → Abschnitt „S2-Speed-Messung") — der S2-SPEED-*Mechanismus* funktioniert wie entworfen (10-min-Hard-Cap feuerte, ehrliches Teilergebnis gepostet, Live-Progress gestreamt, answer-before-markers, kein falscher State), aber das *Latenz-Ziel* (b) wurde auf diesem Lauf verfehlt. Isolierte Kind-Instanz (Port 3125, PID 10100, build `34b0c65`, agency, Session-Vars gestrippt, `detectExistingInstance`→null, Orphan-Reaper env-neutralisiert, eigener s2speed-Lock, TASKS_FILE/JOBS_FILE isolierte Kopien; Driver las die Konversation aus der isolierten tasks.json auf :3125 zurück → Child schrieb nur dort). Exakte Circle-Statusfrage per `POST /api/tasks/proj-zurich-circle-hublcr/log`. **(a) Stufe-1 = 73,8s < 90s-MUSS** ✅ (Cold, `deepVerification.required=true`, Deep-Job gequeued). **(b) Stufe-2-Nachtrag erschien nach 10,0 min** (deep-runner `deepDurationMs=600.518` ≈ 10-min-Hard-Cap / 11,27 min ab POST) → **überschreitet ≤5-min-Ziel UND ≤8-min-akzeptabel**; `followupStatus=partial`. **Hard-Cap gerissen → ehrliches Teilergebnis gepostet** ✅ („hit the 10-minute hard cap; this is a partial result, not a new asserted task state", „Still open"-Liste, Marker 0/0 → kein State mutiert). **(c) Live-Fortschritts-Updates während Stufe 2 = JA** ✅ (22 `job.progress`-Events, „Checking MyApprovals…", elapsedMs+workIqCalls, final „Partial result posted."). **(d) Antwort korrekt = JA** ✅ **keine falschen offenen Freigaben** (Mail-read/unread als nicht-autoritativ behandelt, keine Invoice als offen behauptet, Radon-9.3E-Sign-off korrekt live als geschlossen verifiziert, MyApprovals-Systemcheck ehrlich als unvollständig markiert). **Root Cause der Latenz-Verfehlung = umgebungsbedingt, KEINE Code-Regression:** Corp-VPN disconnected (MSFTVPN-Manual + MSFT-AzVPN-Manual = Disconnected), `myapprovals.microsoft.com` nicht auflösbar (interner Host) → der Live-MyApprovals-Portal-Check, den der Vor-Audit (18,3 min, vollständig, Netz vorhanden) noch abschloss, konnte nicht fertigwerden; der Agent schleifte bis zum Cap auf Portal-CDP-Probes (WorkIQ-Budget nur 3/6 → zeit-, nicht budget-limitiert; Sekundär-Beobachtung: kein Fail-Fast bei unerreichbarem Portal). **Empfehlung (non-blocking):** mit verbundenem VPN nachmessen (Bedingung, unter der der Live-Check abschliesst); Fail-Fast bei unerreichbarem `myapprovals`/`msapprovals`, damit ein inbox-basiertes Teilergebnis deutlich vor dem 10-min-Cap kommt. **Sicherheit:** kein STOP/START, keine breiten/namensbasierten Kills, nur eigene Kind-PID 10100 (cmdline `node _s2speed-server.mjs` verifiziert) per PID beendet; :3000 (PID 38084, uptime durchgehend 13.035s, Lock unverändert) unangetastet — echte tasks.json wurde im ~12-min-Fenster zwar geändert (`E13F1D64…E8F1457` → `3E01699C…0011C808`, mtime 12:28:32), aber durch die **:3000-Eigenaktivität** (scan-Job + laufender `proj-seestrasse-356`-log-Job via :3000/api/jobs), NICHT durch das isolierte Child (env-Override-Kopien, Root-Datei nie geöffnet); Temp-Harness (`_s2speed-server.mjs`, `.s2speed-tmp\`, s2speed-Lock, brain-work-Run-Dirs) rückstandsfrei entfernt. Nur die zwei Docs (AUDIT-TWOTIER.md, STATE.md) geändert, kein Code (`git status` sauber).

## Vorheriger Stand: ZWEI-STUFEN-CHAT RE-AUDIT NACH FIX **AUDIT-TT: PASS** (AUDIT-TWOTIER.md) — Latenz-Messlatte jetzt erfüllt. Exaktes Protokoll aus der Vor-Audit-Version wiederholt: isolierte Kind-Instanz (Port 3118, pid 29824, agency, Session-Vars gestrippt, `detectExistingInstance`→null, Startup-Orphan-Reaper im Kopie-Server neutralisiert → keine `taskkill` gegen :3000-Subprozesse, eigener audittt-Lock, TASKS_FILE/JOBS_FILE auf isolierte Kopien → echte tasks.json nie geöffnet, SHA-256 `E13F1D64…E8F1457` vor/nach identisch, mtime `2026-07-07 11:17:03` unverändert). Exakte Circle-Frage per `POST /api/tasks/proj-zurich-circle-hublcr/log`, direkt nach erstem `/api/health` (Cold-Start, erste Agency-Invocation; Server-Boot 2,0s). **Fix wirkt:** Stage 1 ist jetzt state-only + MCP-frei (`mcpMode:'none'` → `--no-config-plugins --disable-builtin-mcps --disable-mcp-server workiq`, WorkIQ-Hardlimit 0) plus deterministischer `pmStatus`+FactSheet-Fallback. **(1) Circle Cold = 49,9s** (brain 49,5s, `method=agency-task-chat-fast-v1`, kein Fallback nötig) **< 90s-MUSS** (Vor-Audit: **181,1s** Cold FAIL / 120,1s warm) — führt mit Projektstand „no tracked action items … userActions empty (from project state, last verified 2026-12-01)", flaggt Anixter-Invoices (PO 0101577907: 5735759312/5735759192/5735759222) + Radon-9.3E-Sign-off als „signal only — unverified", nennt Sodexo 5735844555 „approved 6 Jul", Status inline, kündigt Deep-Verify an, `deepVerification.required=true`. **(2) Stage-2 deep_verify** (Job `a7e566d0`) **= 18,3 min < 25-min-Fenster**, markers 5/5/0, confidence high — **live** MSApprovals via **isolierte Debug-Edge + WAM-SSO + CDP** (Pending „No Pending Requests" + History), **alle** 3 Anixter-Invoices **Approved** (22/24 Jun) + Sodexo Approved (6 Jul) → **keine falschen offenen Freigaben**; Radon-9.3E-Sign-off **live als GESCHLOSSEN verifiziert** („sign-off approved" 25 Jun 14:50, Outlook) — stärker als der Yardstick; Dorota-Wareneingang (16 Jun, PO 0101577907) ehrlich in Review geroutet statt als offen behauptet; Status inline. **(3) Trivial-Frage „Summarize this task in two sentences" = 39,7s, `deepVerification.required=false`, KEIN Stage-2.** **(4) Timeout-Notausgang** per Injection (`_runBrain → {ok:false,timedOut:true,salvaged:false}`) = **48ms** deterministischer Fallback (`method=agency-task-chat-fast-fallback-v1`), führt mit „Current project state:", Status inline, kündigt Deep-Verify an — **keine No-Answer-Fehlerlage** (genau der behobene Cold-Start-Defekt); zusätzlich per `npm test` abgedeckt. **Regression: npm test 137/137.** Non-Blocking: `deepVerification.completedAt` weiterhin fehl-gestempelt (= Deep-Start statt Deep-Ende, Display-only). **Parallele Session (nicht-blockierend, KEIN Audit-Artefakt):** Während des Audits lief eine separate, eigenständig user-beauftragte Codex-„S2-SPEED"-Implementer-Session (pids 26392/17248/21312, `codex exec … -m gpt-5.5`, Start 11:44:57) mit dem Auftrag, den Stufe-2-Deep-Verify-Pfad zu beschleunigen (Antwort-vor-Marker, Stufe-1-„verify exactly"-Fokusliste, Live-Fortschrittszeile, ~5-min-Ziel + 10-min-Hard-Cap, WorkIQ-Budget 6, `S2-SPEED: OK` an RESULT-TWOTIER.md). Sie editierte zeitgleich `brain/task-chat.js`, `index.html`, `tests/unit/twotier-chat.mjs`. **Diese Änderungen gehören dieser Session, nicht diesem Audit** — meine Messungen sind unberührt, weil der isolierte Kind-Server `task-chat.js` beim Boot (~11:26) VOR Codex' Edits (11:44:57) lud (alle 4 Tests liefen gegen committed `64f0d99` in-memory); tasks.json ebenfalls von Codex unangetastet. Ich habe die Codex-Session **nicht** beendet/gestört (Regel „laufende Instanzen/fremde Sessions nicht anfassen"). Ehrliche Anmerkung: beim ersten Bemerken einer unerwarteten task-chat.js-Änderung habe ich sie kurz per `git checkout` zurückgesetzt, bevor ich die parallele Session erkannte; Codex hat seine Arbeit erneut angewandt, danach liess ich seine Dateien unangetastet. **Dieses Audit ändert nur die zwei Dokumente** (AUDIT-TWOTIER.md, STATE.md). Sicherheit: kein STOP/START, keine breiten/namensbasierten Kills, nur eigene Kind-PID 29824 (+ 1 conhost) per expliziter PID beendet, :3000 (pid 38084, uptime durchgehend 8900s→10445s) + Codex-Session (26392/17248/21312) + echte tasks.json/Lock unangetastet, Temp-Harness rückstandsfrei entfernt.

## Vorheriger Stand: ZWEI-STUFEN-CHAT FINALTEST **AUDIT-TT: FAIL** (vor Fix) — Latenz-Messlatte verfehlt. Isolierte Kind-Instanz (Port 3118, pid 25128, agency, Session-Vars gestrippt, `detectExistingInstance`→null, eigener audittt-Lock, TASKS_FILE/JOBS_FILE auf isolierte Kopien → echte tasks.json nie geöffnet, SHA-256 `ED8AC58C…4541731` vor/nach identisch). Exakte Circle-Frage per `POST /api/tasks/proj-zurich-circle-hublcr/log`. **FAIL-Grund = Stage-1-Latenz der Approval/Status-Frage:** <90s-MUSS nie erreicht (warm **120,1s**, brain 118,3s), harte <180s-Grenze beim **Cold-Start gebrochen** (**181,1s** Fast-Timeout, Salvage <200 Bytes fehlgeschlagen, Job `failed` `Agency brain run timed out`, **keine Antwort** ausgeliefert, kein Conversation-Entry, kein Deep-Job). **Alles Inhaltliche + Trivial-Pfad + Stage-2 = PASS:** (Stage-1-Inhalt) führt mit Projektstand „no open user actions … from project state (last verified 2026-12-01)", Inbox-Signale „signal only — unverified", keine erledigte Freigabe als offen, kündigt Deep-Verify an, Status inline; (Stage-2 deep_verify, Job `bb94dbb3`, ~14–16 min < 25-min-Fenster, markers 5/5/0, confidence high) hat **live** den MSApprovals-Portal-Check via **isolierte Debug-Edge + WAM-SSO + CDP** durchgeführt (aspirierter Normalpfad, den AUDIT-8 noch als Notausgang-Hedge sah), bestätigt **alle** Invoices (PO 0101577907: 5735759192/24 Jun, 5735759312/24 Jun, 5735759222/22 Jun; PO 0101608497: 5735844555/6 Jul; MyOrder PO 101689105/26 Jun) als **Approved** → **keine falschen offenen Freigaben**, nennt **Radon-9.3E-Sign-off** (Sorina Fota, Teams) als einzigen möglichen offenen Punkt, Status inline; (Trivial-Frage „Summarize this task in two sentences") **47,2s, `deepVerification.required=false`, KEIN Stage-2** → Fast-Pfad ist schnell, wenn kein WorkIQ nötig. **Root Cause (behoben):** `buildTaskChatFastPrompt` erlaubte bis zu 2 WorkIQ-Lookups; das Modell machte den **Inbox-Scan inline in Stage-1** (~40–90s je Lookup) statt ihn nach Stage-2 zu verschieben → sprengte 90s (und cold den 180s-Salvage). Fix: Stage 1 state-only + MCP-frei + deterministischer Fallback (Commit `64f0d99`), re-auditiert oben als AUDIT-TT: PASS.

## Vorheriger Stand: BATCH 8/8B ABSCHLUSS-AUDIT **AUDIT8: PASS** (AUDIT-BATCH-8.md) — Direktvergleich der Circle-Approval-Frage nach Brain-Gedächtnis + Wahrheitshierarchie + Antwort-Disziplin. Isolierte Kind-Instanz (Port 3117, pid 42524, agency, Session-Vars gestrippt, `detectExistingInstance`→null, eigener audit8-Lock, **TASKS_FILE/JOBS_FILE auf isolierte Kopien** → echte `tasks.json` nie geöffnet). Exakte Nutzer-Frage per `POST /api/tasks/proj-zurich-circle-hublcr/log` → Job `900f4aed` (brain_run→brain_gateway→completed, ~7,5 min, WorkIQ real aufgerufen, Inbox-Scan 22 Jun–7 Jul). Antwort **kehrt die alte Fehlantwort um**: (C1) führt mit verifizierten Fakten, „verifizierte Punkte zuerst", unverifizierte klar als „Candidates — could NOT be verified" getrennt; (C2) Invoice-Freigaben ehrlich „**unverified via system of record**" + Verweis auf **MyApprovals** + „I'm not asserting a state" (ehrlicher Unverified-Pfad; kein automatisierter Edge-CDP-Portal-Check — einziger Abstand zum Ideal, nicht-blockierend); (C3) **keine** bereits erledigte Freigabe als offen behauptet — die 3 Anixter/Wesco-Invoices (PO 0101577907) NICHT als offen dargestellt („could mean already actioned … not asserting a state"); (C4) nennt den **Radon-9.3E-Sign-off** an Sorina Fota prominent (25 Jun, verbatim Ask-Quote, „Act now"); (C5) Verifikationsstatus **inline** je Aussage. Marker: `markersParsed=3`, der Radon-`userAction`-`PROJECT_UPDATE` wurde vom Batch-7-Action-Gate **gehalten** (askQuote ohne vollständige verbatim-Struktur) → reviewQueue +1, **keine** falsche userAction/Invoice-State persistiert; abgeleitete `confidence='low'` ist reines Held-Marker-Metadaten-Artefakt. **8B-Direktvergleich:** paralleler Audit-Executor (agency pid 42316 via bash) hat zeitgleich die **:3000**-Instanz gegen die **echte** tasks.json bechattet (agency-Kind 16008, ppid 38084) — meine Kopie-Isolation verhinderte jede Kreuz-Kontamination. Sicherheit: kein STOP/START, keine breiten Kills, nur eigene Kind-PID 42524 beendet, :3000 (pid 38084) + parallele agency-Prozesse + echte tasks.json/jobs.json unangetastet, Temp-Harness rückstandsfrei entfernt.

## Vorheriger Stand: BATCH 7 ABSCHLUSS-AUDIT **AUDIT7: PASS** (AUDIT-BATCH-7.md) — Wahrheitsbaum + Action-Gate + Ledger. Adversarial in BEIDE Richtungen: (Falsch-Positiv) Fall A + Fall B aus allen sichtbaren Flächen weg (0 userActions, factSheet-openActions mit removedAt+state obsolete/superseded+resolvedBy/obsoleteEvidence, 3 Renderer filtern !removedAt), verlustfrei archiviert (34 reviewQueue-Einträge + fs-b7-Status-Fakten + Ledger), Fall A unabhängig per workiq bestätigt stale (@-Mention 06.06 → "Donnerstag"=11.06 vergangen, Thread lief bis 25.06). (Falsch-Negativ, kritisch) Selbst-workiq-Suche letzte 14 Tage + Ziel-Probes auf entfernte Kandidaten: "AV sign-off requested by Patrick" hat KEINE direkte Adressierung, PO 101577907 aufgelöst 19.02.2026 → beide korrekt entfernt, kein Falsch-Negativ; einziger ambiger Rest (SAP 5735236710/Innovation-Hub, 20.02, ausserhalb Projekte+Fenster) non-blocking. Isolierter Live-Scan (Kind :3141 pid 46148, agency, Session-Vars gestrippt) → completed/partial: Ledger-Qualitäts-Gate feuerte ("missing ledger disposition for email:src-jit-039008") → appliedMarkers=0, 0 falsche Actions, Fall A/B nicht wieder da, Cursor NICHT vorgerückt (fail-safe D1/D3). Ledger 34/34 valide Dispositionen, Header "Processed up to" vorhanden. (Konflikt) disputed-Mechanismus in Code (truth-tree.validateNodeState ≥2 positions, marker-applier.syncConflictProblems "Conflicting information", index.html renderConflictPanel) + grünem Test belegt. npm test 113/113. Nutzer-Instanz :3000 (pid 40156) durchgehend unangetastet, kein STOP/START, nur eigene Kind-PID beendet, tasks.json NICHT verändert (Scan gegen isolierte Kopie), Temp-Harness rückstandsfrei entfernt.

## Vorheriger Stand: BATCH 6 + 6B ABSCHLUSS-AUDIT **AUDIT6: PASS** (AUDIT-BATCH-6.md) — Sweep sigma-safe (Σ identisch über 4 Snapshots, 0 dangling, 0 Fabrikate; 8 Marker = 7 no-op updatedAt + 1 substantiell Circle-SAP), 4/4 Stichproben per workiq-ask mailbox-exakt (Circle SAP 5735844555/Sodexo/CHF 26'645.85/PO 0101608497, Dual-MPR Basement/Dez 2026, Seestrasse Temp-WS ~18/17-28 Aug/Laith Skeik, CAB 02-Jul-Session); Owner-Explizitheit (0 Fremd-Owner in userActions, Validator+UI), Tickbox (Carry-Forward + PATCH-by-id + 2-Zustand-Reconcile), Task-Chat (EIN Gateway-Apply-Pfad code-bewiesen, 10min, Multiline, Bild-Paste `--attachment` + Guards), ENGLISH-ONLY (Prompts+UI, 0 Deutsch in generierten Feldern), regressionsfrei (npm test 104/104, tasks.json-Hash unverändert). 2 Non-Blocking-Hinweise: Sweep `lastWorkIqCalls:0` (flache Re-Verifikation) + vorbestehende CAB-Aussage "CHG0858097 completed" nur als "Scheduled" belegbar — keine Batch-6-Regression. Nutzer-Instanz :3000 (pid 26068) unangetastet, kein Kind-Server, keine STOP/START-Skripte.

## Vorheriger Stand: BATCH 5 LIVE-AUDIT **AUDIT5: PASS** (nach Gateway-Fix `71f4c9e`) — Reality-Gateway-Parser gehärtet (prosa-präfixiertes pretty-JSON + Zeilen-Verdikt-Vertrag + 1 Retry → hold-all); Fix-Live-Scan `scan-1783332440027` wandte offensichtlich zugehörige Infos auf die richtigen Projekte an (Circle +1 sourceRef/+2 factSheet, Seestrasse +1/+2, CAB unverändert), Faktensheets fortgeschrieben, 3/3 Stichproben per workiq-ask mailbox-bestätigt, 0 Fabrikate, 0 Fremd-Zuordnung, reviewQueue 18/18 mit Begründung; npm test 91/91

| Schritt | Status |
|---|---|
| 1. Ist-Analyse (4 parallele Agenten) | ✅ done — FACTS.md |
| 2. Unabhängige Pläne (Codex + agency) | ✅ done |
| 3. Debatte + Adjudikation (Master) | ✅ done — DECISION.md (D1–D10) |
| 4a. Slice-0-Gate (Default-Flags) | ✅ PASSED — workiq connected, echte Antwort, exit 0 |
| 4b. Ratifizierung durch agency | ✅ **GO-WITH-CONDITIONS** — RESULT-RATIFICATION.md, Auflagen A1–A6 |
| 4c. Auflage A1: Probe mit exakten D10-Flags | ✅ done |
| 5. Implementierung Batch 1 (Slices 1–4, Codex) | ✅ done — Commits 7fd0ecd…53c5263, 30/30 Tests |
| 5b. Verifikation Batch 1 (4-Dim-Review + Refute-Pass) | ✅ done — 19 Findings → FINDINGS-BATCH-1.md |
| 6. Implementierung Batch 2 (Fix F1–F19 + Slices 5–7) | ✅ done — batch-2 verified CLEAN (P2) |
| 7. Bestandskonsolidierung (Slice 8, Dry-Run→Audit→Apply) | ✅ done — AUDIT: GO, apply b8d4253 |
| 8. Slice 9 (Legacy-SDK/WorkIQ-0.2.8-Gating) | ✅ done — RESULT-CODEX-IMPL-4, 68/68 Tests |
| 9. **Slice 10 — Live-Verifikation (Serie C, C-1..C-6)** | ✅ **FINAL: PASS** — FINAL-VERIFICATION.md |
| 10. A5 — Engine-Default-Flip auf agency (Startskripte) | ✅ done — Start-WorkIQ-Scan.ps1 + START-AGENT-ZERO.bat |
| 11. Batch 5 — FactSheet + Reality-Check-Gateway + Datenreparatur (Codex) | ✅ done — RESULT-BATCH-5.md `BATCH5: OK`, RESULT-FIX-CIRCLE.md, npm test 85/85 |
| 12. Batch-5 Live-Audit (agency, D-Kriterien am echten Bestand) | ⚠️ historisch: AUDIT5: FAIL (2e/2f) — Gateway strict-JSON-Parser fail-closed hielt alle Belegupdates; behoben durch `71f4c9e` |
| 13. **Gateway-Fix (`71f4c9e`) + fokussiertes Re-Audit** | ✅ **AUDIT5: PASS** — AUDIT-BATCH-5.md; 2e+2f erfüllt (persistierter Delta + workiq-Mailbox 3/3), Kriterium 3 regressionsfrei (0 Fabrikate, 0 Moerken/Norway, reviewQueue 18/18 mit Grund), Kriterium 4 (auflage 4) per Tests belegt (kaputte Zeile hält nur diese; 1 Retry; dann hold-all), npm test 91/91 |
| 14. Batch 6 — Re-Verifikations-Sweep + Owner + Tickbox + Task-Chat (Codex) | ✅ done — RESULT-BATCH-6.md `BATCH6: OK markers=8 applied=8`, Commits `6b8b2dc`/`8684e68`/`0b40168` |
| 15. Batch 6b — Task-Chat Multiline + Bild-Paste + English-only (Codex) | ✅ done — RESULT-BATCH-6B.md `BATCH6B: OK`, Commits `96535fc`/`13a2c20`/`347d239`, npm test 104/104 |
| 16. **Batch 6 + 6b Abschluss-Audit (agency, 6 Kriterien am echten Bestand)** | ✅ **AUDIT6: PASS** — AUDIT-BATCH-6.md; Sweep Σ-safe + 4/4 workiq-Mailbox exakt, Owner/Tickbox/Task-Chat/English/Regression alle belegt (code+UI+tests), npm test 104/104, tasks.json unberührt; 2 Non-Blocking-Hinweise (Sweep-Tiefe `lastWorkIqCalls:0`, CAB-"completed"-Über-Assertion) |
| 17. Batch 7 — Thread-vollständige Verarbeitung + Action-Item-Gate + Ledger + disputed (Codex) | ✅ done — RESULT-BATCH-7.md `BATCH7: OK 34/0`, Repair-Sweep applied, npm test 113/113 |
| 18. **Batch 7 Abschluss-Audit (agency, beide Richtungen + isolierter Live-Scan)** | ✅ **AUDIT7: PASS** — AUDIT-BATCH-7.md; Fall A+B weg & verlustfrei archiviert (workiq-bestätigt stale), Falsch-Negativ ausgeschlossen (entfernte Kandidaten stale/nicht-adressiert per workiq), Live-Scan Quality-Gate fail-safe (0 falsche Actions, Cursor nicht vorgerückt), Ledger 34/34 valide + Header, disputed code+test; npm test 113/113, :3000 unangetastet, tasks.json unberührt |
| 19. Batch 8 + 8B — Brain-Gedächtnis (brain-learnings.md) + Wahrheitshierarchie + Antwort-Disziplin (Codex) | ✅ done — RESULT-BATCH-8.md `BATCH8: OK` / `BATCH8B: OK`, npm test 127/127 |
| 20. **Batch 8/8B Abschluss-Audit (Direktvergleich, isolierte Kind-Instanz gegen Circle-Approval-Frage)** | ✅ **AUDIT8: PASS** — AUDIT-BATCH-8.md; Antwort kehrt alte Fehlantwort um: verifiziert-zuerst, Invoices ehrlich „unverified via system of record" (MyApprovals), keine erledigte Freigabe als offen, Radon-Sign-off genannt, Verifikationsstatus inline; Action-Gate hielt unvollständigen Radon-userAction-Marker (0 falsche State-Persistenz); 8B-parallel-Executor gegen :3000/echte tasks.json ohne Kreuz-Kontamination (Kopie-Isolation); :3000 unangetastet, echte tasks.json nie geöffnet |
| 21. Batch 9 — ENTFESSELUNG (Käfig-Flags entfernt, Tool-Budget warn 40/stop 150, Anhang-Pflicht-Evidenz, Wahrheitsbaum-Injektion) (Codex) | ✅ done — RESULT-BATCH-9.md `BATCH9: OK`, npm test 142/142 |
| 22. **Batch 9 Abnahme-Audit (Direktvergleich per Brief §5, isolierte Kind-Instanz gegen Seestrasse-Inbox-Update)** | ❌ **AUDIT9: FAIL a,d** — AUDIT-BATCH-9.md; (c) Stufe-1 31,8 s < 90 s ✅ + (b) Gateway approved 7/7, nur wirklich geänderte Knoten, DLP-Signal→Review ✅; ABER (a) PDF-/Deck-Anhang nicht geöffnet → PIS 31. Aug + 24-Port-Panel-Mitigation fehlen komplett (½-Tag-Vorbehalt Nicolas frisch geerntet, restliche Fakten vorbestehend), (d) verstrichener AV-Go-Live 1. Jul nicht als stale erkannt/bereinigt (steht unverändert in pmStatus.planned/li-see-av-commissioning); Gateway bemängelte weder ungelesenen Anhang noch stale Go-Live; :3000 (PID 21644) + echte tasks.json (SHA vor/nach identisch) unangetastet, Temp-Harness rückstandsfrei entfernt |

## Batch-5 Live-Audit (2026-07-06, AUDIT-BATCH-5.md)
- **(1) STATIC PASS:** 0 fabrizierte `turn*search*`-Links in 24 aktiven Tasks; Circle
  `proj-zurich-circle-hublcr` 0 Moerken/Norway (Kontamination reversibel in reviewQueue, 13 Einträge);
  3 Faktensheets englisch + kanonische 12-Sektionen; 5/5 Stichproben plausibel (4× workiq-Mailbox
  bestätigt, 1× Seestrasse via read-only report.html, patch-panel workiq DLP-blockiert).
- **(3) UI PASS:** `/api/tasks/:id/factsheet.html` HTTP 200, 12 Sektionen, dd.mm.yyyy-Daten +
  Evidence-Links, kein `Invalid Date`, keine Fabrikate.
- **(2) LIVE — behoben, jetzt PASS (e+f):** Ursprünglicher isolierter Scan :3101 (runId
  `scan-1783328859868`) hielt fail-closed 5/6 Marker (intermittenter ~50%-Parser-Fehler). Nach
  Gateway-Fix `71f4c9e` wandte der Fix-Live-Scan `scan-1783332440027` (Child :3104) 7 Marker sauber
  an: **Circle** sourceRefs 22→23 + factSheet 25→27, **Seestrasse** sourceRefs 33→34 + factSheet
  59→61, CAB unverändert, reviewQueue 18→18. Persistiert im Live-Bestand; 3/3 Stichproben per
  workiq-ask mailbox-bestätigt (Dual-MPR-Parcels/Andreas Arnold, Invoice 5735844555/Approved,
  Seestrasse-Temp-Workspace/Martin Hämmerli).
- **Root cause (behoben):** `brain/reality-gateway.js extractJson` — prosa-präfixiertes pretty-JSON
  defeat der 3 Extraktionspfade. Fix `71f4c9e`: Zeilen-Verdikt-Vertrag `GATEWAY_DECISION<TAB>idx<TAB>
  decision<TAB>reason` + gehärtete balanced-brace `"decisions"`-Extraktion als JSON-Fallback +
  genau 1 Reformat-Retry vor hold-all (kaputte Einzelzeile hält nur diese) + actionable
  Technik-Reason + APPLIED-statt-Intent-Telemetrie. Re-Audit-Beleg: AUDIT-BATCH-5.md.
- **Audit-Hygiene:** Nutzer-Instanz :3000 (pid 26068) unangetastet (uptime durchgehend, Lockfile
  unverändert); nur eigene Kind-PID beendet; keine STOP/START-Skripte, keine breiten Kills, keine
  Scan-Überlappung.

## Schlussstand (2026-07-05, Slice 10)
- **Alle 6 Serie-C-Kriterien bestanden** unter produktionsäquivalenter Isolation
  (FINAL-VERIFICATION.md). 3/3 isolierte Live-Scans mit `SCAN_DONE`, 0 Duplikate, bestehende
  Projekte aktualisiert, genuine neue Singles angelegt; Datenintegrität perfekt (keine Task
  gelöscht, Σ History 798→800, Σ Links 221→243).
- **Umgebungsartefakt dokumentiert (kein Agent-Zero-Defekt):** Zwei anfängliche Scan-Timeouts
  entstanden durch vererbte `AGENCY_SESSION_ID`/`COPILOT_AGENT_SESSION_ID` (Session-Bleed,
  weil die Verifikation aus einer aktiven agency-CLI-Session lief). Behoben durch Serverstart
  ohne diese Variablen (= Produktionszustand unter Task Scheduler). Optionale Härtungs-
  Empfehlung: `buildAgencyEnv()` könnte diese Variablen aus dem Brain-Kind-Env strippen.
- **A5 vollzogen:** `AGENT_ZERO_SCAN_ENGINE` defaultet jetzt in beiden Startskripten auf
  `agency` (env-Override auf `legacy` bleibt möglich; C-5 Rückschaltung verifiziert).
- **Kosten:** 45 geloggte premiumRequests (3 erfolgreiche Scans × 15); zusätzlich verbrauchten
  die 2 Timeout-Scans + 2 Proben nicht-surfaced Premium.
- **Offene, bewusste Nicht-Aktionen für P7 (Codex/Master):** README/AGENTS.md-Sync, CHANGELOG,
  Version 5.0.0; optionale `buildAgencyEnv()`-Härtung; ggf. SDK/WorkIQ-0.2.8-Dependency-
  Entfernung, sobald der Legacy-Flag-Pfad final zurückgebaut werden soll.

## Status vor Slice 10 (Referenz): PHASE 5 — RATIFIZIERT (GO-WITH-CONDITIONS)

## Auflagen aus der Ratifizierung (bindend: A1, A2)
- **A1** (vor Slice 1/5): D10-Flag-Kombination live verifizieren (`--no-default-mcps` darf workiq nicht droppen)
- **A2** (vor Slice 8/Abnahme): Abnahmetests an D6 anpassen — AV+LAN+Patch-Panel+Switch-Ports = Line Items EINES Seestrasse-Projekts; nur genuine Fremd-Themen getrennt
- **A3**: Datum-only-Evidenz ⇒ confidence ≤ medium + Datum muss auf SourceRef desselben Runs zurückführen
- **A4**: Renderer schreibt State-Doc + Spill-Dateien nach brain-work; brain-work pro Run anlegen/leeren, .gitignore, von Prozess-Cleanup ausnehmen
- **A5**: Engine-Default bleibt `legacy` bis Slice-8-Apply auditiert; Flip auf `agency` erst Slice 10
- **A6**: Höheres WorkIQ-Budget nur für den einmaligen Migrationslauf; premiumRequests separat loggen

## Adjudizierte Kernentscheide (Details in DECISION.md)
- D1: Projekt = Task (`taskType:"project"`, lineItems[], strukturiertes pmStatus mit Evidenz je Eintrag)
- D2: Brain macht Discovery selbst via geerbtem workiq-MCP; keine Signal-Inbox
- D3: 8 Marker inkl. NEEDS_REVIEW statt ASK_USER; Evidenz-Gate fail-closed
- D4: --max-autopilot-continues 0 (empirisch prüfen, SCAN_DONE-Quote messen)
- D5: --add-dir NUR brain-work\ (Brain kann tasks.json physisch nicht erreichen)
- D6: Seestrasse-Umbau = EIN Projekt mit Line Items (Nutzer-Mentalmodell bindend)
- D8: Migration Dry-Run → Audit durch agency → Apply; Invarianten Σ History/Σ Links
- D9: Slice-Reihenfolge 0–10; SDK-Entfernung erst nach Live-Verifikation

## Kernbefunde der Ist-Analyse (Kurzform, Details in FACTS.md)
- Kein Projekt-Konzept im Datenmodell; 1 Task pro E-Mail/Teams-Nachricht.
- Konsolidierung (Phase 4) erzeugt nur Vorschläge, die der geplante Scan verwirft;
  CONSOLIDATE_SKILL.md primt aktiv GEGEN Seestrasse-Gruppierung (Anti-Beispiel Z. 21).
- @github/copilot-sdk (frische Session pro Call, kein Modell-Pin, thread-scoped) statt
  agency copilot; WorkIQ 0.2.8 statt 1.0.0.
- Headless `agency copilot -p … --allow-all-tools -s` funktioniert (getestet, exit 0).
- Bug: Start-WorkIQ-Scan.ps1 sendet `days`, Server liest `scanDays` → wirkungslos.

## Entscheidungen (ratifiziert)
- (noch keine — warten auf Pläne)

## Offene Design-Fragen (für die Debatte)
1. Brain-Topologie: 1 globaler Scan-Run vs. pro Projekt vs. Hybrid
2. Sessions: fresh pro Run vs. --session-id/resume
3. Schicksal des WorkIQ-0.2.8-Eigenclients (~700 Z. Härtung)
4. Datenmodell v5 (lineItems-Feldliste) + Migration der 76 Bestands-Tasks
5. Marker-Set + Validierung
6. UI-Darstellung Projekt-Tasks (PM-Sicht) in der SPA
7. Kostenkontrolle (premiumRequests pro Scan)
8. Brain-System-Prompt (Projektmanager-Denke, generisch — kein Seestrasse-Wissen einkodieren)

## Artefakte
- `MISSION.md` — Ziele G1–G7 (Abnahmekriterien)
- `FACTS.md` — code-verifizierte Faktenbasis
- `PROMPT-CODEX-PLAN.md` / `PROMPT-AGENCY-PLAN.md` — Planungsaufträge
- `RESULT-CODEX-PLAN.md` / `RESULT-AGENCY-PLAN.md` — erwartet
- Grundwahrheit für Tests: `E:\Task_Zero 03\projects\zurich-seestrasse-av-lan-tracker\deliverable\seestrasse-status-report.html` (READ-ONLY, nichts einkodieren)
