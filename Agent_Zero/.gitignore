# Test results - do not publish
tests/
test-results/

# Node modules
node_modules/

# Local data
tasks.json
scan-log.txt

# Non-publishable assets
docs/archive/
docs/Final Video/
*.docx

# Optimization workspace — contains sensitive test data (real inbox content)
Agent_Zero_Claude_Loop/

# Environment variables
.env
.env.*
.env.local
.env.production

# MSAL token cache (contains auth tokens — never commit)
.msal-token-cache.json

# Debug logs (may contain M365 content)
logs/

# Playwright MCP session artifacts (screenshots, snapshots — not project files)
.playwright-mcp/

# Squad/agent session artifacts
.squad/
.github/agents/
